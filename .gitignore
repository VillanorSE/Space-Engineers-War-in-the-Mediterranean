# C# / MDK2 build artifacts (WW2-WitM-WaterAuto and any future compiled mods)
bin/
obj/
.vs/
*.user
*.sbcB5
MDK/

# This repo tracks the mods themselves plus Roadmap/Modlist/README only.
# Generation tools, working data, and other non-mod material live in
# Documents\Claude_SE_Project_Files\ instead -- if a Tools/ folder like the
# planet pipeline's ever ends up under a mod dir again, it belongs moved
# out, not committed. This entry is just a safety net.
**/Tools/
